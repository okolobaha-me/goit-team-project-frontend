import {Container} from '../../baseStyles';
import {Graph} from '../../components/Graph/Graph';

const Statistics = () => {
    return (
        <Container>
            <Graph />
        </Container>
    );
};

export default Statistics;
